<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ramadan 2026 Paris - App Quotidienne</title>
    <style>
        :root {
            --terracotta: #E2725B;
            --sable: #F4E4BC;
            --dore: #D4AF37;
            --fond: #FDF6E3;
            --texte: #5D4037;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: 'Segoe UI', Tahoma, sans-serif; 
            background: linear-gradient(to bottom, var(--sable), var(--fond)); 
            color: var(--texte); 
            min-height: 100vh; 
            padding: 20px; 
        }
        header { 
            text-align: center; padding: 30px 20px; background: var(--terracotta); 
            color: white; border-radius: 20px; margin-bottom: 30px; box-shadow: 0 8px 20px rgba(226,114,91,0.3);
        }
        h1 { font-size: 2.5em; margin-bottom: 10px; text-shadow: 2px 2px 4px rgba(0,0,0,0.3); }
        h2 { color: var(--dore); border-bottom: 2px solid var(--dore); padding-bottom: 10px; margin-bottom: 20px; }
        .calendar { 
            background: white; border-radius: 15px; padding: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); 
            overflow-x: auto; margin-bottom: 30px;
        }
        table { width: 100%; border-collapse: collapse; }
        th { background: var(--terracotta); color: white; padding: 15px; font-weight: bold; }
        td { padding: 12px 10px; text-align: center; border-bottom: 1px solid #eee; cursor: pointer; transition: all 0.3s; }
        td:hover { background: var(--sable); }
        .today { background: var(--dore) !important; color: white; font-weight: bold; border: 3px solid var(--terracotta) !important; }
        .content { 
            display: grid; grid-template-rows: auto auto auto; gap: 30px; max-width: 800px; margin: 0 auto;
        }
        .section { 
            background: white; border-radius: 15px; padding: 25px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); 
            animation: fadeIn 0.5s ease-in;
        }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .hadith { font-style: italic; font-size: 1.1em; color: var(--terracotta); border-left: 5px solid var(--dore); padding-left: 20px; }
        .recipe { background: var(--sable); padding: 20px; border-radius: 10px; }
        .todo { list-style: none; }
        .todo li { 
            background: linear-gradient(90deg, var(--terracotta), var(--dore)); color: white; 
            margin: 10px 0; padding: 15px; border-radius: 10px; position: relative; cursor: pointer;
        }
        .todo li::before { content: '☀️'; margin-right: 10px; }
        .todo li.done { opacity: 0.7; text-decoration: line-through; }
        .dons { text-align: center; margin-top: 40px; }
        .don-btn { 
            display: inline-block; background: var(--dore); color: white; padding: 15px 30px; 
            margin: 10px; border-radius: 30px; text-decoration: none; font-weight: bold; transition: all 0.3s;
        }
        .don-btn:hover { transform: scale(1.05); box-shadow: 0 5px 15px rgba(212,175,55,0.4); }
        @media (max-width: 768px) { 
            h1 { font-size: 2em; } table { font-size: 14px; } .content { grid-template-rows: auto; gap: 20px; }
        }
    </style>
</head>
<body>
    <header>
        <h1>🌙 Ramadan 2026 Paris</h1>
        <p>17 février - 19 mars 1447 | Suivez votre journée spirituelle</p>
    </header>

    <div class="calendar">
        <h2>Calendrier Interactif Paris <span id="currentDay"></span></h2>
        <table>
            <thead>
                <tr>
                    <th>Jour</th><th>Date FR</th><th>Hijri</th><th>Fajr</th><th>Iftar</th><th>Isha</th>
                </tr>
            </thead>
            <tbody id="calendarBody">
                <!-- Rempli par JS -->
            </tbody>
        </table>
    </div>

    <div class="content">
        <div class="section">
            <h2>📖 Hadith du Jour</h2>
            <div id="hadith" class="hadith">Cliquez sur un jour pour découvrir !</div>
        </div>

        <div class="section">
            <h2>🍲 Recette Équilibrée</h2>
            <div id="recipe" class="recipe">Sélectionnez un jour pour une recette saine !</div>
        </div>

        <div class="section">
            <h2>✅ To-Do Liste du Jour</h2>
            <ul id="todoList" class="todo"></ul>
        </div>
    </div>

    <div class="dons">
        <h2>💖 Soutenez le Ramadan</h2>
        <a href="#" class="don-btn" onclick="alert('Lien don 1 à ajouter')">Don Muslim Hands</a>
        <a href="#" class="don-btn" onclick="alert('Lien don 2 à ajouter')">Secours Islamique</a>
        <a href="#" class="don-btn" onclick="alert('Lien don 3 à ajouter')">Votre lien perso</a>
    </div>

    <script>
        // Données Paris 2026 [page:0][web:3]
        const calendarData = [
            {jour:1, date:'17/02/2026', hijri:'1 Ramadan', fajr:'06:50', iftar:'18:20', isha:'19:40'},
            {jour:2, date:'18/02/2026', hijri:'2 Ramadan', fajr:'06:48', iftar:'18:21', isha:'19:42'},
            {jour:3, date:'19/02/2026', hijri:'3 Ramadan', fajr:'06:46', iftar:'18:22', isha:'19:44'},
            // ... J4-29 abrégés pour espace (ajoute depuis [page:0] : progressif Fajr~05:20, Iftar~19:00)
            {jour:27, date:'15/03/2026', hijri:'27 Ramadan', fajr:'05:30', iftar:'18:55', isha:'20:25'},
            {jour:30, date:'18/03/2026', hijri:'30 Ramadan', fajr:'05:26', iftar:'18:58', isha:'20:30'}
        ];

        // Hadiths [web:6]
        const hadiths = [
            "« Le jeûne est un bouclier. Il protège du Feu comme le feu protège de la terre. » (Ahmad) [web:6]",
            "« Quiconque jeûne un jour pour Allah, Allah l'éloigne du Feu de 70 années. » (Bukhari) [web:6]",
            "« L'odeur de la bouche du jeûneur est plus agréable à Allah que le parfum. » (Bukhari) [web:6]"
            // Ajoute 27+ depuis [web:6]
        ];

        // Recettes équilibrées [web:33]
        const recipes = [
            "Harira protéinée : lentilles, pois chiches, agneau maigre, légumes. 500cal, hydratant.",
            "Salade quinoa-feta : quinoa, concombres, tomates, feta light, huile olive. Protéines végé.",
            "Poulet pruneaux cannelle : blanc poulet, pruneaux, légumes vapeur. Équilibré glucides/protéines."
            // 27+ rotations
        ];

        // To-Do jour par jour
        const todos = {
            1: ["Prières surérogatoires (2 rakats avant Fajr)", "Bienfait: Multiplie les récompenses x27", "Du'a: 'Allahoumma aj'al houwa farjan wa tatawwa' beihi'", "Lire Sourate Al-Fatiha x7"],
            2: ["Faire un don (zakat/ sadaqa)", "Prières surérogatoires (4 rakats Dohr)", "Lire Sourate Al-Ikhlas x100", "Dhikr: Subhanallah 100x"],
            3: ["Lire 1 Juz' Coran", "Visiter famille/inviter iftar", "Prières surérogatoires Asr", "Du'a protection famille"],
            // J4-20: similaires progressifs (dhikr, Coran, charité)
            27: ["Tarawih complète + Witr", "Du'as Laylat al-Qadr spécifiques", "Qiyam toute nuit possible", "Zakat Fitr calculée", "Don spécial (boutons ci-dessous)"]
            // J28-30: Aïd prep, etc.
        };

        // Remplir calendrier
        const tbody = document.getElementById('calendarBody');
        calendarData.forEach((day, index) => {
            const tr = document.createElement('tr');
            tr.onclick = () => loadDay(day.jour);
            tr.innerHTML = `<td>${day.jour}</td><td>${day.date}</td><td>${day.hijri}</td><td>${day.fajr}</td><td>${day.iftar}</td><td>${day.isha}</td>`;
            tbody.appendChild(tr);
        });

        // Charger jour
        function loadDay(jour) {
            document.getElementById('hadith').textContent = hadiths[(jour-1) % hadiths.length];
            document.getElementById('recipe').innerHTML = `<strong>Jour ${jour}:</strong> ${recipes[(jour-1) % recipes.length]}`;
            
            const todoList = document.getElementById('todoList');
            todoList.innerHTML = '';
            (todos[jour] || ['To-do par défaut : Jeûner + Prier']).forEach((item, i) => {
                const li = document.createElement('li');
                li.textContent = item;
                li.onclick = () => li.classList.toggle('done');
                todoList.appendChild(li);
            });

            // Reset autres jours
            document.querySelectorAll('#calendarBody tr').forEach(row => row.classList.remove('today'));
            event.target.closest('tr').classList.add('today');
            document.getElementById('currentDay').textContent = `> Jour ${jour}`;
        }

        // Auto-sélectionner "aujourd'hui" (ajuste pour date réelle Ramadan)
        loadDay(1); // Démo J1
    </script>
</body>
</html>
